description: The password and the repetition of the password don't match.
content:
  application/json:
    schema:
      type: object
      properties:
        code:
          type: number
          example: 409
        message:
          type: string
          example: The password and the repetition of the password don't match.
